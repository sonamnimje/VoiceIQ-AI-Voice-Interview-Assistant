fastapi
uvicorn
flask
flask-socketio
python-socketio
openai
python-dotenv
pydub
# Voice and Audio Processing
speechrecognition
# PyAudio fails to build on many Linux CI/build images because it
# requires the system PortAudio headers and native build tools.
# Install pyaudio only on non-Linux platforms (Windows/macOS).
# See: https://github.com/intxcc/pyaudio_portaudio
pyaudio; sys_platform == "win32" or sys_platform == "darwin"
webrtcvad
soundfile
scipy
# AI and ML
transformers
torch
torchaudio
opencv-python
# File Processing
python-multipart
Pillow
PyPDF2
python-docx
# Additional utilities
numpy
pandas
scikit-learn
# WebRTC / Async IO
aiortc
aiofiles
# PDF Generation
reportlab
# NOTE: mediapipe removed because no wheel available for Python 3.13 on PyPI.
# If you require mediapipe, use Python 3.11 or add a custom build/install step.